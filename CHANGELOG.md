# 更新日志

本文件记录 Video Summary 的公开版本变化。

## 1.2.0 - 2026-09-21

「本地任务」可以直接从挂载目录挑选文件，不用先把视频复制到浏览器里上传。

### 新增

- 「本地任务」支持从挂载目录挑选文件：新增 `GET /api/jobs/local-root`（决定是否显示入口）与 `GET /api/jobs/local-entries`（列目录 / 递归扫描，支持按名称搜索与分页）。界面把「本地上传」和「从挂载目录选择」做成原生单选（默认本地上传），后者在弹框里浏览、搜索、翻页、多选文件（勾选文件夹即递归加入其全部文件）；未配置 `MEDIA_DIR`（compose 变量 `VIDEO_SUMMARY_MEDIA`）时不显示该入口
- 「本地任务」一次最多创建 1000 个任务、单个文件夹最多扫 1000 个文件，超出会提示

## 1.1.0 - 2026-09-20

知识库从视频扩展到文档与在线网址，站点式拉取支持 B 站空间 / 小鹅通店铺 / 约牛整站，任务支持多选与汇总总结。

### 新增

- 知识库支持文档与在线网址：PDF / Word / Markdown / TXT / HTML，以及 generic 适配器下的普通网页
- 文档默认直接入库原文，任务页勾选「文档生成 AI 总结」后才走模型；章节可按页/段定位
- 任务详情可预览文档原件：PDF 内嵌并按页跳转，网页优先显示可静态渲染的原文（微信正文解开隐藏、补上懒加载图片）；正文只在 NUXT/JSON 里的站点则还原文章 HTML，避免把导航壳当成原文；仍不行再退回提取文本；Word 转成简易 HTML
- 扫描件 OCR、旧版 `.doc` 做成数据目录插件，设置页可安装/卸载，默认镜像不打进 Tesseract/LibreOffice
- A 股预设词汇表补全沪深京公司名简称：去掉字母后三字保留、四字及以上取前两字并去重
- 知识库问答支持历史记录：按领域保存在本机，可搜索、删除，刷新后仍可继续对话
- 定时任务全部完成后可生成汇总总结：把各条章节按主题合并成一份综述任务，详情可跳回原任务；任务列表多选也可汇总；原任务已在知识库中，汇总本身不入库
- 任务多选时按选中项状态禁用不可用的批量操作：汇总需至少两个已完成任务，取消仅处理中，重试仅失败或已取消
- 视频任务综述章节标题后的时间改为片子时钟，点击可跳到对应播放位置；结构化综述下不再重复展示分段章节

### 修复

- 任务列表取消多选时，已选操作栏不再因按钮消失而高度抖动
- B 站定时拉取改为优先走 WBI 稿件列表，限流后加长退避并停止继续扫后续 UP，避免把额度打爆
- 未开启「每天定时拉取」时，应用启动和后台循环都不会自动扫描；同一天定时最多执行一次
- 定时扫描只新建当天发布的内容，不会用昨天的稿件凑满数量；B 站已拉到条目或额度已满后，日志不再提示「请等几分钟再试」
- 从定时任务日志进入任务详情后可返回该页；任务不存在会提示并在 3 秒后跳回
- B 站限流不再弹 toast，日志会提示等几分钟再试，或到站点页填写 Cookie 保持登录态
- 立即执行会立刻显示「正在扫描」和进行中日志，扫完再更新结果；部分成功不再用成功提示；不会把上一轮日志当成这一轮结果
- 站点页 Cookie 支持直接粘贴多行导出格式（每行一个 `name=value`）：失焦和保存时自动合并成单行并去掉「Cookie:」前缀，B 站等站点不再报 `Illegal header value`
- 自定义请求头里的换行、制表符会在发请求前折叠成空格，避免粘贴内容把请求打挂
- 任务详情播放区的接口报错改用 toast 提示，不再在播放提示里闪一下又被播放器错误盖掉

## 1.0.0 - 2026-09-05

首个正式版本。本地视频、流媒体转写与 AI 时间轴总结工作台可以独立部署使用。

### 新增

- 支持本地文件、公开 HTTP 视频、HLS，以及可配置登录态的站点页面
- 接入 B 站公开稿件、小鹅通短链与约牛/加菲财经直播页适配
- 本机 SenseVoice / Whisper 转写，也可改用 OpenAI 兼容云端转写接口
- 大模型按分段索引生成总结，程序回填真实时间码，点击即可跳回原片
- 已完成转写可入库，按内容领域做本机知识库问答
- 站点登录档案、内容领域包、转写词汇表与自动 AI 校对可在界面配置
- 可在设置页配置每天定时拉取：按站点内容源扫描新视频，跳过已有任务，并查看简单运行日志。B 站多个 UP、小鹅通多个店铺可分行填写，也可在站点页添加多条
- 支持多主题外观，设置页可查看版本与免责声明
- 提供 Docker Compose、离线镜像打包，以及 GitHub Packages 容器镜像

### 说明

- 只使用你手动配置的 Cookie / Header，不会绕过付费墙或解密 DRM
- 采用 PolyForm Noncommercial License 1.0.0，商业使用须事先取得书面许可
