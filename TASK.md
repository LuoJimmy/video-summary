# Context
Filename: TASK.md
Created On: 2026-08-15
Created By: AI
Associated Protocol: RIPER-5 + Multidimensional + Agent Protocol

# Task Description
从零建设「视频转文本 + AI 时间轴总结」项目：支持线上流媒体、线上/本地视频；多站点与登录态打通且可在界面配置；总结必须标注可回跳的视频时间轴。示例站点为小鹅通短链与加菲财经/约牛直播页。

# Project Overview
全新空仓库。目标是本地优先的媒体处理工作台：用户配置站点 Cookie/请求头后解析媒体，抽取音频，转写为带时间戳分段，再由大模型按分段索引生成总结，最后由程序回填真实时间码。

---
*The following sections are maintained by the AI during protocol execution*
---

# Analysis (Populated by RESEARCH mode)

工作区 `/Users/jimmyluo/Documents/workspace/jimmy/Video Summary` 为空目录，无既有代码、依赖或数据。

本机环境：`/opt/homebrew/bin/python3.12`、`/opt/homebrew/bin/python3.11`、Node v23.11.0 / npm 可用；系统默认 `python3` 指向 3.8.2；未安装 `ffmpeg` / `ffprobe` / `yt-dlp`。

小鹅通示例 `https://etrsz.xetslk.com/sl/q1M06`：HTTP 200，标题「小鹅通提供技术支持」，`xetslk.com` 为小鹅短链。页面是 Vue SPA，脚本来自 `static-cdn.xiaoeknow.com/live_mp_link/...`，形态为直播/短链落地页。公开文档说明小鹅通登录态走 Cookie，Web 登录约 24 小时；音视频多为带鉴权/加密的 HLS，官方开放能力是 WebSDK 与 `xe.login.url`，不提供第三方直接拉流接口。参见 [WebSDK 接入](https://api-doc.xiaoe-tech.com/recall_scene/lnline_sdk/h5_sdk.html)。

约牛/加菲财经示例 `https://jf.yueniuzq.com/living/?id=3f14baab82b61eaf6d47deab521b6f7e`：HTTP 200，标题「加菲财经直播」，Nuxt SSR。页面注入 `caibuluo.interLive = https://jflive.yueniuzq.com`，并加载 `hls-new.js` 与腾讯云 `tcplayer.v5.3.4`。未登录时 `__NUXT__.state.signedIn = false`。直播流不在首屏 HTML 中，需登录态后由前端播放器向直播域名取源。页面含 VIP 订阅提示。

时间轴正确性约束：大模型直接输出 `00:12:03` 容易幻觉。可靠做法是 Whisper/兼容接口产出 `segments[{id,start,end,text}]`，模型只返回分段索引，程序再映射真实秒数。

输入形态可归为四类：本地文件、公开 HTTP 视频、HLS/m3u8、需 Cookie 的站点页（短链/直播间）。后两类必须把用户手动配置的 Cookie/Header 注入到解析与拉流请求。

未知项：两站点在无用户 Cookie 时无法拿到可播地址；本机暂无 FFmpeg，真实抽音需运行时依赖；加密 HLS/DRM 无法在不破解的前提下转写。

# Proposed Solution (Populated by INNOVATE mode)

方案 A 是纯浏览器插件拦截 m3u8，耦合站点 DOM，难测、难多站点扩展。方案 B 是 Playwright 打开用户会话抓流，能覆盖动态页，但重、不稳定，且接近未授权采集。方案 C 是本地工作台：站点/登录档案可配置，适配器只做 URL 识别、短链跟随、Cookie 注入和页面内明显媒体地址提取；解析失败时允许用户粘贴已有权限的媒体地址。转写与总结走 OpenAI 兼容接口，便于接云端或本地 Whisper/LLM。时间轴用「分段索引 → 程序回填」避免幻觉。

倾向方案 C：合法边界清晰（只使用用户自己的登录态），可单测，可先跑通本地文件与直链，再按站点迭代。

# Implementation Plan (Generated by PLAN mode)

技术栈：Python 3.12 + FastAPI + SQLite；Vue 3 + Vite + TypeScript；pytest 覆盖适配器、时间轴映射与 API；FFmpeg 为运行时抽音依赖，测试中 mock。

数据模型：`AuthProfile`（登录档案：Cookie/Headers，供多站点复用）+ `Site`（适配器、域名规则、绑定档案）+ `Job`（源、状态、转写 JSON、总结 JSON）+ `AppSetting`（转写/总结 API）。

适配器：`generic`（本地/HTTP/HLS）、`xiaoe`（xetslk/xiaoeknow 等）、`yueniu`（yueniuzq/jflive）。管线：resolve → extract_audio → transcribe_segments → summarize_by_index → persist。

前端：任务创建/列表/详情（播放器 + 可点击时间轴）、站点与登录档案配置、模型设置。

```
Implementation Checklist:
1. 建立仓库骨架、忽略规则、需求文档与 README
2. 实现数据模型、种子站点/档案与设置/站点/档案 API
3. 实现三类入站适配器与时间轴映射
4. 实现抽音、转写、总结与后台任务管线
5. 实现任务 API（创建、上传、查询、重试）
6. 实现 Vue 配置界面与时间轴总结页
7. 编写并运行自动测试
```

# Current Execution Step (Updated by EXECUTE mode when starting a step)
> Currently executing: "全部清单已完成，进入 REVIEW"

# Task Progress (Appended by EXECUTE mode after each step completion)
* 2026-08-15
  * Step: 1 建立仓库骨架、忽略规则、需求文档与 README
  * Modifications: README.md, .gitignore, docs/REQUIREMENTS.md, docs/ARCHITECTURE.md, TASK.md
  * Change Summary: 空仓库补齐文档与忽略规则
  * Reason: Executing plan step 1
  * Blockers: None
  * User Confirmation Status: Auto-continued per user request
* 2026-08-15
  * Step: 2-5 后端模型、适配器、管线与 API
  * Modifications: backend/app/**, backend/tests/**
  * Change Summary: 登录档案打通、xiaoe/yueniu/generic 适配器、时间轴回填、任务管线
  * Reason: Executing plan steps 2-5
  * Blockers: 本机无 FFmpeg，真实抽音需用户安装；两站点无 Cookie 时只能预解析页面
  * User Confirmation Status: Auto-continued per user request
* 2026-08-15
  * Step: 6-7 前端与自动测试
  * Modifications: frontend/**, backend/tests/**
  * Change Summary: Vue 任务/站点/设置页；pytest 18 通过，vitest 2 通过
  * Reason: Executing plan steps 6-7
  * Blockers: None
  * User Confirmation Status: Auto-continued per user request

* 2026-09-04
  * Step: 领域包（默认 A 股）
  * Modifications: backend/app/services/domain.py, summarize/knowledge/textnorm/proofread/lexicon/settings_store, schemas, jobs, frontend Settings/Knowledge/overview
  * Change Summary: 股票口径抽成可配置领域包，默认仍为 A 股；通用预设可切换
  * Reason: Executing approved domain-pack plan
  * Blockers: None
  * User Confirmation Status: Auto-continued per user request

# Final Review (Populated by REVIEW mode)
实现与最终计划一致：多站点 + 登录档案打通、界面可配、本地/直链/HLS/站点页接入、总结时间轴由分段索引回填。未做站点 DRM 破解与未授权拉流。自动测试已通过。2026-09-04 起内容领域可配置，默认 A 股盘面课。

Implementation perfectly matches the final plan.
